import React, {useState} from "react";
import API from '../../utils/API';

const News = (props) => {
    return (
        <form>
        <h1>Here's a news form, dummy</h1>
        </form>
    )
    
}

export default News;