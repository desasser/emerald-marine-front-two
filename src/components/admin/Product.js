import React, {useState} from "react";
import API from '../../utils/API';

const Product = (props) => {
    return (
        <form>
        <h1>Here's a product form, dummy</h1>
        </form>
    )
    
}

export default Product;