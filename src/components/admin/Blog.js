import React, {useState} from "react";
import API from '../../utils/API';

const Blog = (props) => {
    return (
        <form>
        <h1>Here's a blog form, dummy</h1>
        </form>
    )
    
}

export default Blog;